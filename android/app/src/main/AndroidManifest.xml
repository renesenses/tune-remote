<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Decouverte des serveurs Tune en mDNS / DNS-SD.

         bonsoir_android passe par NsdManager et prend un WifiManager
         MulticastLock : les deux permissions ci-dessous sont exactement celles
         que la bibliotheque declare dans son propre manifeste
         (bonsoir_android/android/src/main/AndroidManifest.xml). Le fusionneur
         les ajouterait tout seul ; on les ecrit ici pour qu'elles soient
         lisibles dans le manifeste de l'application.

         Ni NEARBY_WIFI_DEVICES ni ACCESS_FINE_LOCATION ne sont demandees :
         NsdManager n'appelle aucune API Wi-Fi qui les exige, et les demander
         ferait apparaitre une boite de dialogue systeme pour rien.

         INTERNET n'etait declaree que dans les manifestes debug et profile :
         sans elle, la version release ne peut ni chercher ni joindre le
         serveur. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE"/>
    <application
        android:label="Tune Remote"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
